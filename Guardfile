# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{app/(views|helpers|sections)/.+})
  watch(%r{(app|vendor)/assets/\w+/.+})
  watch(%r{spec/javascripts/.+})
end

